{
  "buildCommand": "prisma generate --schema server/prisma/schema.prisma && vike build",
  "outputDirectory": "dist/client",
  "framework": null,
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/index.js" },
    { "source": "/(.*)", "destination": "/api/ssr.js" }
  ]
}